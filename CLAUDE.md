### Dotfiles

This repo is built to make it easy to spin up a dev environment with a lot of the default software which is useful day to day.

### Basic guidelines
It uses `poe` as its task runner. Run `poe` to see what it exposes (and consider adding tasks if there are commands which will be used very regularly).

Read READMEs in the folder you're looking at when answering.
